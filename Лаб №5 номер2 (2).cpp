
using namespace std;

int main()
{
	double x

		for (x = 1; x <= 3.1; x = x + 0.1)

		{
		double n = pow(sin(x); 3) + pow((pow(x; 2) - 1); 1 / 4);
		double d = 3 * x;
		double	f = n / d;



		cout << "f=" << f << endl;
		}


	return 0;

}